╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║         MIGRATE CLIENTS TO SUPABASE - QUICK GUIDE            ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

📍 FILE TO EDIT: src/App.tsx

┌───────────────────────────────────────────────────────────────┐
│ CHANGE 1: Add Import (top of file)                           │
├───────────────────────────────────────────────────────────────┤
│ ADD after the mockData import:                                │
│                                                               │
│   import { clientService } from './services/clientService';  │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ CHANGE 2: Update State (line ~57)                            │
├───────────────────────────────────────────────────────────────┤
│ BEFORE:                                                       │
│   const [clients, setClients] = useState(mockClients);       │
│                                                               │
│ AFTER:                                                        │
│   const [clients, setClients] = useState<Client[]>([]);      │
│   const [isLoadingClients, setIsLoadingClients] =            │
│     useState(true);                                           │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ CHANGE 3: Add Load Function (after currentUserId)            │
├───────────────────────────────────────────────────────────────┤
│ ADD this entire block:                                        │
│                                                               │
│   useEffect(() => {                                           │
│     loadClients();                                            │
│   }, []);                                                     │
│                                                               │
│   async function loadClients() {                              │
│     try {                                                     │
│       setIsLoadingClients(true);                              │
│       const data = await clientService.getAll();              │
│       setClients(data);                                       │
│       console.log('✅ Loaded', data.length, 'clients');       │
│     } catch (error) {                                         │
│       console.error('❌ Error:', error);                      │
│       showToast('Failed to load clients', 'error');           │
│       setClients(mockClients); // Fallback                    │
│     } finally {                                               │
│       setIsLoadingClients(false);                             │
│     }                                                          │
│   }                                                            │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ CHANGE 4: Update Add Function (search for handleAddContact)  │
├───────────────────────────────────────────────────────────────┤
│ BEFORE:                                                       │
│   const handleAddContact = (client: Client) => {              │
│     setClients([...clients, client]);                         │
│     showToast(`Added ${client.name}`, 'success');             │
│   };                                                           │
│                                                               │
│ AFTER:                                                        │
│   const handleAddContact = async (clientData) => {            │
│     try {                                                     │
│       const newClient = await clientService.create(           │
│         clientData);                                          │
│       setClients([...clients, newClient]);                    │
│       showToast(`Added ${newClient.name}`, 'success');        │
│     } catch (error) {                                         │
│       console.error('Error:', error);                         │
│       showToast('Failed to add client', 'error');             │
│     }                                                          │
│   };                                                           │
└───────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

✅ SAVE FILE → AUTO RELOAD → CHECK CONSOLE

═══════════════════════════════════════════════════════════════

🎯 THEN ADD TEST DATA IN SUPABASE:

Supabase → SQL Editor → New Query → Paste:

  INSERT INTO clients (name, contact_person, email, phone, 
    location, notes)
  VALUES 
    ('Hope Foundation', 'Sarah Johnson', 
     'sarah@hope.org', '555-0101', 'NY', 'Education'),
    ('Community Care', 'Michael Brown', 
     'mike@care.org', '555-0102', 'LA', 'Healthcare'),
    ('Green Earth', 'Emma Davis', 
     'emma@earth.org', '555-0103', 'Seattle', 'Environment');

Click RUN → Refresh CRM → See 3 organizations! 🎉

═══════════════════════════════════════════════════════════════

📋 DETAILED GUIDE: UPDATE_APP_INSTRUCTIONS.md

═══════════════════════════════════════════════════════════════
